# SPDX-License-Identifier: MIT
name: ANTSDR OS E310 build

on:
  workflow_call:
    inputs:
      version:
        required: false
        type: string
      source_date_epoch:
        required: false
        type: string
        default: ''
      uenv_mode:
        required: false
        type: string
        default: compat
      build_label:
        required: false
        type: string
        default: release
      toolchain_only:
        required: false
        type: boolean
        default: false
      runner_mode:
        required: false
        type: string
        default: hosted
    outputs:
      artifact_name:
        description: Name of the uploaded build artifact
        value: ${{ jobs.build.outputs.artifact_name }}
    secrets:
      R2_S3_API_URL:
        required: true
      R2_BUCKET:
        required: true
      R2_INSTALLER_KEY:
        required: true
      R2_ACCESS_KEY_ID:
        required: true
      R2_SECRET_ACCESS_KEY:
        required: true
      AMD_USERNAME:
        required: true
      AMD_PASSWORD:
        required: true
  workflow_dispatch:
    inputs:
      version:
        description: ANTSDR OS major.point version
        required: true
        type: string
      source_date_epoch:
        description: Reproducible Unix timestamp; blank uses the commit timestamp
        required: false
        default: ''
        type: string
      uenv_mode:
        description: U-Boot uEnv compatibility mode
        required: true
        default: compat
        type: choice
        options: [compat, locked]
      build_label:
        description: Unique label for repeated builds
        required: true
        default: manual
        type: string
      toolchain_only:
        description: Populate or verify the segmented AMD toolchain cache without building firmware
        required: true
        default: false
        type: boolean
      debug_enabled:
        description: Temporarily open a tmate shell after Vivado and source setup
        required: true
        default: false
        type: boolean
      runner_mode:
        description: Build runner and AMD toolchain storage mode
        required: true
        default: hosted
        type: choice
        options: [hosted, aliyun-persistent]

permissions:
  contents: read

concurrency:
  group: antsdr-os-build-${{ github.workflow }}-${{ github.ref }}-${{ inputs.build_label || 'push' }}
  cancel-in-progress: false

jobs:
  build:
    name: Build E310 Rev.C
    runs-on: ${{ inputs.runner_mode == 'aliyun-persistent' && 'self-hosted' || 'ubicloud-standard-4-ubuntu-2204' }}
    timeout-minutes: 90
    outputs:
      artifact_name: ${{ steps.metadata.outputs.artifact_name }}
    env:
      TZ: UTC
      LANG: C
      LC_ALL: C
    steps:
      - name: Check out build configuration
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Resolve AMD toolchain cache identity
        if: inputs.runner_mode != 'aliyun-persistent'
        id: amd_cache
        shell: bash
        run: |
          set -euo pipefail
          digest="$(sha256sum \
            ci/vivado/web-installer.env \
            ci/vivado/install_config.txt.in \
            ci/vivado/toolchain-cache.sh | sha256sum | awk '{print $1}')"
          printf 'key=vivado-toolchain-x64-ubuntu2204-v3-%s\n' "$digest" >> "$GITHUB_OUTPUT"
      - name: Resolve AMD toolchain location
        id: toolchain_location
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ inputs.runner_mode }}" = aliyun-persistent ]; then
            install_root="$HOME/.local/share/antsdr-os/Xilinx"
            printf 'ANTSDR_PERSISTENT_TOOLCHAIN=1\n' >> "$GITHUB_ENV"
          else
            install_root="$GITHUB_WORKSPACE/.toolchains/Xilinx"
          fi
          printf 'XILINX_INSTALL_ROOT=%s\n' "$install_root" >> "$GITHUB_ENV"
          printf 'install-root=%s\n' "$install_root" >> "$GITHUB_OUTPUT"
      - name: Prepare hosted build host
        if: inputs.runner_mode != 'aliyun-persistent'
        shell: bash
        run: |
          set -euo pipefail

          sudo systemctl stop docker.service docker.socket containerd.service 2>/dev/null || true
          sudo rm -rf \
            /usr/local/lib/android \
            /usr/share/dotnet \
            /opt/ghc \
            /usr/share/swift \
            /var/lib/docker \
            /opt/hostedtoolcache/CodeQL

          sudo apt-get update
          sudo apt-get install --no-install-recommends -y \
            bc \
            bison \
            build-essential \
            cpio \
            device-tree-compiler \
            expect \
            file \
            flex \
            libelf-dev \
            libmpc-dev \
            libncurses5 \
            libssl-dev \
            libtinfo5 \
            rsync \
            unzip \
            wget \
            x11-utils \
            xauth \
            xvfb \
            zstd
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          command -v aws
      - name: Prepare persistent build host
        if: inputs.runner_mode == 'aliyun-persistent'
        shell: bash
        run: |
          set -euo pipefail
          packages=(
            awscli
            bc
            bison
            build-essential
            ca-certificates
            cpio
            curl
            device-tree-compiler
            expect
            file
            flex
            git
            gnupg
            libelf-dev
            libmpc-dev
            libncurses5
            libssl-dev
            libtinfo5
            rsync
            unzip
            wget
            x11-utils
            xauth
            xvfb
            xz-utils
            zstd
          )
          missing=()
          for package in "${packages[@]}"; do
            dpkg-query -W -f='${Status}' "$package" 2>/dev/null | grep -qx 'install ok installed' || \
              missing+=("$package")
          done
          if (( ${#missing[@]} )); then
            sudo apt-get update
            sudo apt-get install --no-install-recommends -y "${missing[@]}"
            sudo apt-get clean
            sudo rm -rf /var/lib/apt/lists/*
          fi
          command -v aws
          command -v xlsclients
          command -v Xvfb
          command -v xvfb-run
      - name: Restore toolchain cache manifest
        if: inputs.runner_mode != 'aliyun-persistent'
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/manifest
          key: ${{ steps.amd_cache.outputs.key }}-manifest-lookup-${{ github.run_id }}
          restore-keys: |
            ${{ steps.amd_cache.outputs.key }}-manifest-
      - name: Read toolchain cache generation
        if: inputs.runner_mode != 'aliyun-persistent'
        id: cache_index
        shell: bash
        run: |
          set -euo pipefail
          manifest="$GITHUB_WORKSPACE/.toolchains/cache/manifest/toolchain.env"
          generation=""
          if [ -r "$manifest" ]; then
            candidate="$(sed -n 's/^generation=//p' "$manifest")"
            if [[ "$candidate" =~ ^[0-9]+$ ]]; then
              generation="$candidate"
            fi
          fi
          printf 'generation=%s\n' "$generation" >> "$GITHUB_OUTPUT"
      - name: Restore toolchain cache part 00
        if: inputs.runner_mode != 'aliyun-persistent' && steps.cache_index.outputs.generation != ''
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-00
          key: ${{ steps.amd_cache.outputs.key }}-part-00-${{ steps.cache_index.outputs.generation }}
      - name: Restore toolchain cache part 01
        if: inputs.runner_mode != 'aliyun-persistent' && steps.cache_index.outputs.generation != ''
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-01
          key: ${{ steps.amd_cache.outputs.key }}-part-01-${{ steps.cache_index.outputs.generation }}
      - name: Restore toolchain cache part 02
        if: inputs.runner_mode != 'aliyun-persistent' && steps.cache_index.outputs.generation != ''
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-02
          key: ${{ steps.amd_cache.outputs.key }}-part-02-${{ steps.cache_index.outputs.generation }}
      - name: Restore toolchain cache part 03
        if: inputs.runner_mode != 'aliyun-persistent' && steps.cache_index.outputs.generation != ''
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-03
          key: ${{ steps.amd_cache.outputs.key }}-part-03-${{ steps.cache_index.outputs.generation }}
      - name: Restore AMD FPGA toolchain from cache
        if: inputs.runner_mode != 'aliyun-persistent'
        id: toolchain_cache
        shell: bash
        run: |
          set -euo pipefail
          toolchain="$XILINX_INSTALL_ROOT"
          cache="$GITHUB_WORKSPACE/.toolchains/cache"
          cache_hit=false
          if [ -n "${{ steps.cache_index.outputs.generation }}" ] && \
             bash ci/vivado/toolchain-cache.sh restore "$toolchain" "$cache"; then
            cache_hit=true
          else
            rm -rf "$toolchain" "$cache/parts"
          fi
          printf 'cache-hit=%s\n' "$cache_hit" >> "$GITHUB_OUTPUT"
      - name: Detect persistent AMD FPGA toolchain
        if: inputs.runner_mode == 'aliyun-persistent'
        id: persistent_toolchain
        shell: bash
        run: |
          set -euo pipefail
          present=false
          if [ -r "$XILINX_INSTALL_ROOT/Vitis/2023.2/settings64.sh" ] && \
             [ -x "$XILINX_INSTALL_ROOT/Vivado/2023.2/bin/vivado" ] && \
             [ -x "$XILINX_INSTALL_ROOT/Vitis/2023.2/bin/xsct" ] && \
             [ -x "$XILINX_INSTALL_ROOT/Vivado/2023.2/bin/bootgen" ]; then
            present=true
          fi
          printf 'present=%s\n' "$present" >> "$GITHUB_OUTPUT"
      - name: Install AMD FPGA toolchain
        if: >-
          (inputs.runner_mode == 'aliyun-persistent' && steps.persistent_toolchain.outputs.present != 'true') ||
          (inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true')
        shell: bash
        env:
          R2_S3_API_URL: ${{ secrets.R2_S3_API_URL }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_INSTALLER_KEY: ${{ secrets.R2_INSTALLER_KEY }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AMD_USERNAME: ${{ secrets.AMD_USERNAME }}
          AMD_PASSWORD: ${{ secrets.AMD_PASSWORD }}
        run: bash ci/vivado/install-vivado.sh
      - name: Activate existing AMD FPGA toolchain
        if: >-
          (inputs.runner_mode == 'aliyun-persistent' && steps.persistent_toolchain.outputs.present == 'true') ||
          (inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit == 'true')
        shell: bash
        env:
          AMD_TOOLCHAIN_PRESENT: 1
        run: bash ci/vivado/install-vivado.sh
      - name: Pack AMD FPGA toolchain cache
        if: inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          set -euo pipefail
          bash ci/vivado/toolchain-cache.sh pack \
            "$XILINX_INSTALL_ROOT" \
            "$GITHUB_WORKSPACE/.toolchains/cache" \
            "$GITHUB_RUN_ID"
      - name: Save toolchain cache part 00
        if: inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-00
          key: ${{ steps.amd_cache.outputs.key }}-part-00-${{ github.run_id }}
      - name: Verify toolchain cache part 00
        if: inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true'
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-00
          key: ${{ steps.amd_cache.outputs.key }}-part-00-${{ github.run_id }}
          lookup-only: true
          fail-on-cache-miss: true
      - name: Save toolchain cache part 01
        if: inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-01
          key: ${{ steps.amd_cache.outputs.key }}-part-01-${{ github.run_id }}
      - name: Verify toolchain cache part 01
        if: inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true'
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-01
          key: ${{ steps.amd_cache.outputs.key }}-part-01-${{ github.run_id }}
          lookup-only: true
          fail-on-cache-miss: true
      - name: Save toolchain cache part 02
        if: inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-02
          key: ${{ steps.amd_cache.outputs.key }}-part-02-${{ github.run_id }}
      - name: Verify toolchain cache part 02
        if: inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true'
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-02
          key: ${{ steps.amd_cache.outputs.key }}-part-02-${{ github.run_id }}
          lookup-only: true
          fail-on-cache-miss: true
      - name: Save toolchain cache part 03
        if: inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-03
          key: ${{ steps.amd_cache.outputs.key }}-part-03-${{ github.run_id }}
      - name: Verify toolchain cache part 03
        if: inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true'
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/parts/part-03
          key: ${{ steps.amd_cache.outputs.key }}-part-03-${{ github.run_id }}
          lookup-only: true
          fail-on-cache-miss: true
      - name: Save toolchain cache manifest
        if: inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/manifest
          key: ${{ steps.amd_cache.outputs.key }}-manifest-${{ github.run_id }}
      - name: Verify toolchain cache manifest
        if: inputs.runner_mode != 'aliyun-persistent' && steps.toolchain_cache.outputs.cache-hit != 'true'
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ${{ github.workspace }}/.toolchains/cache/manifest
          key: ${{ steps.amd_cache.outputs.key }}-manifest-${{ github.run_id }}
          lookup-only: true
          fail-on-cache-miss: true
      - name: Clear toolchain cache staging
        if: always() && inputs.runner_mode != 'aliyun-persistent'
        shell: bash
        run: rm -rf "$GITHUB_WORKSPACE/.toolchains/cache"
      - name: Check out pinned component sources
        if: inputs.toolchain_only != true
        shell: bash
        run: |
          set -euo pipefail
          git submodule update --init --recursive --depth 1
      - name: Set up Python
        if: inputs.toolchain_only != true
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: '3.12'
      - name: Install metadata dependencies
        if: inputs.toolchain_only != true
        run: python -m pip install --disable-pip-version-check -r requirements-dev.txt
      - name: Resolve version and source identity
        if: inputs.toolchain_only != true
        id: metadata
        shell: bash
        env:
          INPUT_VERSION: ${{ inputs.version }}
          INPUT_EPOCH: ${{ inputs.source_date_epoch || '' }}
          BUILD_LABEL: ${{ inputs.build_label || 'push' }}
        run: |
          set -euo pipefail
          version="$INPUT_VERSION"
          if [[ "$GITHUB_REF_NAME" == e310-revc-os-* ]]; then
            python tools/release_metadata.py check --tag "$GITHUB_REF_NAME"
            version="${GITHUB_REF_NAME##*-os-}"
          elif [ -z "$version" ]; then
            version="$(python tools/release_metadata.py show --board e310 --format github | sed -n 's/^os_version=//p')"
          fi
          test -n "$version"
          epoch="$INPUT_EPOCH"
          if [ -z "$epoch" ]; then
            epoch="$(git show -s --format=%ct "$GITHUB_SHA")"
          fi
          export SOURCE_DATE_EPOCH="$epoch"
          python tools/release_metadata.py show --board e310 --version "$version" --channel stable > "$RUNNER_TEMP/build-metadata.json"
          python - "$RUNNER_TEMP/build-metadata.json" "$BUILD_LABEL" >> "$GITHUB_OUTPUT" <<'PY'
          import json
          import sys
          metadata = json.load(open(sys.argv[1], encoding="utf-8"))
          label = sys.argv[2]
          print(f"artifact_name={metadata['artifact_stem']}-{label}")
          print(f"version={metadata['os_version']}")
          PY
          echo "SOURCE_DATE_EPOCH=$epoch" >> "$GITHUB_ENV"
          echo "ANTSDR_VERSION=$version" >> "$GITHUB_ENV"
      - name: Temporary Vivado debug shell
        if: github.event_name == 'workflow_dispatch' && inputs.debug_enabled
        uses: mxschmitt/action-tmate@35b54afac29c97fb54faba5b513f8fbd1882f113 # v3
        timeout-minutes: 45
      - name: Build Linux, Buildroot, U-Boot, FPGA and BOOT.BIN
        if: inputs.toolchain_only != true && inputs.debug_enabled != true
        shell: bash
        env:
          UENV_MODE: ${{ inputs.uenv_mode || 'compat' }}
        run: |
          set -euo pipefail
          source "$VIVADO_SETTINGS"
          workspace="$ANTSDR_BUILD_ROOT/e310"
          export ADI_HDL_DIR="$workspace/src/hdl"
          export ANTSDR_OS_VERSION="$ANTSDR_VERSION"
          python tools/e310_build.py all \
            --workspace "$workspace" \
            --jobs "${RUNNER_NUM_CPUS:-4}" \
            --uenv-mode "$UENV_MODE" \
            --xsct "$GITHUB_WORKSPACE/ci/vivado/xsct-headless.sh" \
            --release "$RUNNER_TEMP/antsdr-e310-release" \
            --execute
      - name: Report FPGA project failure
        if: failure() && inputs.toolchain_only != true && inputs.debug_enabled != true
        shell: bash
        run: |
          set -euo pipefail
          log="$ANTSDR_BUILD_ROOT/e310/src/hdl/projects/e310/e310_vivado.log"
          if [ -r "$log" ]; then
            tail -n 1000 "$log"
          else
            printf 'Vivado project log was not created: %s\n' "$log" >&2
          fi
      - name: Attach build metadata and verify assembly
        if: inputs.toolchain_only != true && inputs.debug_enabled != true
        shell: bash
        run: |
          set -euo pipefail
          release="$RUNNER_TEMP/antsdr-e310-release"
          cp "$RUNNER_TEMP/build-metadata.json" "$release/build-metadata.json"
          python tools/verify_release.py --release "$release" --board e310
          mkdir -p "$RUNNER_TEMP/antsdr-e310-package/release"
          cp -a "$release/." "$RUNNER_TEMP/antsdr-e310-package/release/"
      - name: Upload unmodified build output
        if: inputs.toolchain_only != true && inputs.debug_enabled != true
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ${{ steps.metadata.outputs.artifact_name }}
          path: ${{ runner.temp }}/antsdr-e310-package
          if-no-files-found: error
          retention-days: 14
